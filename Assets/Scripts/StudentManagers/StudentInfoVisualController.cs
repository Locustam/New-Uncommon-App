using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StudentInfoVisualController : MonoBehaviour
{
    //!!!!!!!!!!So far not in use!!!!!!!!!!!!!
    //jh this is a controller script created in the makings of New Uncommon App
    // it's purpose is to be the centralized controller that controls all the visual presentation of student info
    //under it is many specific controllers that does detailed jobs such as updating preferences, bools, etc.
    //as this is a reconstruction of the original code, there could be places that is not updated

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
